<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>詳細ページ | Tainer</title>
        <link rel="icon" type="image/x-icon" href="../images/favicon.ico">
        <link rel="apple-touch-icon" sizes="180x180" href="../images/favicon.ico">
        <link rel="stylesheet" href="../css/show.scss">
        <link rel="stylesheet" href="/assets/stylesheets/comments.scss">
        <link rel="stylesheet" href="../css/responsive.css">
        <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.6.3/css/all.css">
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.4/jquery.min.js"></script>
    </head>
    <body>
        <div class="md-frame">
            <div class="md-l">
                <%= render partial: 'boards/menubar' %>
                <%= render partial: 'boards/side_menubar' %>
            </div>
            <div class="md-m">
                <div class="md-prof-a">
                    <div class="md-icon">
                        <img src="/assets/DSC48723@2x.png">
                        <% if @current_user == @board %>
                        <%= link_to 'マイページを編集する', edit_board_path(@board), class: 'edit-link' %>
                        <% end %>
                    </div>
                    <div class="md-name">
                        <h2><%= @board.name %></h2>
                        <!--
                        <div class="md-tag">
                            <h3>#Tag001</h3>
                            <h3>#Tag002</h3>
                            <h3>#Tag003</h3>
                        </div>
                    -->
                    </div>
                </div>
                <div class="md-prof-b">
                    <div class="shape300"></div>
                    <div class="md-profile">
                        <h2>Profile</h2>
                        <div class="md-chara">
                            <p><%= safe_join(@board.profile.to_s.split("\n"),tag(:br)) %></p>
                        </div>
                    </div>
                </div>
            </div>
            <div class="md-r">
                <div class="md-r-inner">
                    <div class="md-r-line"><p>推薦コメント</p></div>
                    <ul class="recommend-area">
                        <%= render @board.comments %>
                    </ul>
                    <div class="recommend-on">
                        <img src="/assets/DSC37894@2x.png">
                        <div class="markup">
                            <div class="markup-inner">
                                <%= render partial: 'comment_form' , locals: {comment: @comment} %>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    <script src="script.js"></script>
    </body>
</html>